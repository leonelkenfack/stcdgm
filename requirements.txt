# =========================
# ST-CDGM requirements — Path C+ version (pinned for reproducibility)
# Audit DS K27: pin all critical packages for cross-environment reproducibility
# =========================

# =========================
# Core scientific stack
# =========================
numpy>=1.24.0,<2.0.0          # K27: avoid numpy 2.0 breaking changes
pandas>=2.0.0,<3.0.0
scipy>=1.11.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

# =========================
# Data formats & processing
# =========================
xarray>=2023.10.0,<2025.0.0
netCDF4>=1.6.0,<2.0.0
h5netcdf>=1.2.0,<2.0.0
zarr>=2.16.0,<3.0.0            # K27: zarr 3.x breaking changes
numcodecs>=0.12.0
dask[complete]>=2023.10.0
webdataset>=0.2.86

# =========================
# Visualization
# =========================
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0
plotly>=5.18.0
cartopy>=0.22.0

# =========================
# PyTorch (GPU support) — pinned for A100 BF16 + torch.compile compatibility
# Path C+ target: PyTorch 2.4+ for stable BF16 on sm_80 and torch.compile inductor
# Install GPU version via:
#   For CUDA 12.1: pip install torch==2.4.* --index-url https://download.pytorch.org/whl/cu121
#   For CUDA 12.4: pip install torch==2.4.* --index-url https://download.pytorch.org/whl/cu124
# =========================
torch>=2.4.0,<2.6.0
torchvision>=0.19.0,<0.21.0
torchaudio>=2.4.0,<2.6.0

# PyTorch Geometric (GNNs for heterogeneous graphs) — A100 BF16 path
torch-geometric>=2.5.0,<2.7.0
# NOTE: torch-scatter/torch-sparse are now optional with PyG >= 2.3
# Install only if needed (long CUDA compile time on Colab):
#   pip install torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.4.0+cu121.html

# =========================
# Data batching and time
# =========================
xbatcher>=0.4.0,<0.5.0
cftime>=1.6.0,<2.0.0

# =========================
# Deep Learning / Diffusion (PINNED — diffusers 0.36 has specific UNet behaviors)
# CRITICAL: do not upgrade diffusers without re-testing cross_attention_dim
# behavior (consensus J11) and pad_mm inductor bug workaround
# =========================
diffusers==0.36.0
transformers==4.57.6
accelerate==1.12.0
huggingface-hub==0.36.0
safetensors==0.7.0

# =========================
# Configuration & utilities (PINNED — OmegaConf merge depth assumptions)
# =========================
hydra-core==1.3.2
omegaconf==2.3.0
pyyaml>=6.0,<7.0
tqdm>=4.66.0
requests>=2.31.0

# =========================
# Notebooks / interactive
# =========================
ipykernel>=6.27.0
ipywidgets>=8.1.0

# =========================
# GPU monitoring (optional)
# =========================
nvidia-ml-py>=12.535.0

# =========================
# Path C+ specific (causal discovery)
# =========================
tigramite>=5.2.0,<6.0.0        # PCMCI+ for Phase B0 offline DAG discovery

# =========================
# Statistical analysis (consensus DS §6)
# =========================
# Bootstrap CI, paired tests, FDR correction
statsmodels>=0.14.0,<0.16.0
